<%- include('partials/head')%>
<body>
    <%- include('partials/header')%>
    
    <main class="usersForm-container">
        
      

        <div class="changePasswordForm">
            <form id="form" action="/user/cambiarContrasenia/<%= user.id %>?_method=PUT" method="POST" enctype="application/x-www-form-urlencoded" class="form-ad" >
                  <div class="titleChangePass">
                    <h3>Cambio de contraseña</h3>
                 </div>
                <!-- Campo Password -->

                <div class="form_user">
                    <input 
                        id="pass" 
                        type="password" 
                        name="pass" 
                        placeholder="Nueva contraseña"
                        class="form_pass"
                        value="<%= locals.old && old.pass %>"
                    />
                    <span id="passErrors" class="text_error">
                        <% if(locals.errors && errors.pass) { %>
                            <%= errors.pass.msg %>
                        <% } %>
                    </span>
                </div>

                <!-- Campo Password 2 -->

                <div class="form_user">
                    <input 
                        id="pass2" 
                        type="password" 
                        name="pass2" 
                        placeholder="Repetir contraseña"
                        class="form_pass"
                        value="<%= locals.old && old.pass2 %>"
                    />
                    <span id="pass2Errors" class="text_error">
                        <% if(locals.errors && errors.pass2) { %>
                            <%= errors.pass2.msg %>
                        <% } %>
                    </span>
                </div>
            
                <!-- Boton de submit -->

            <div class="change_pass"><button class="btn-ad-primary" type="submit">CAMBIAR CONTRASEÑA</button></div> 

            </form>
        </div>
    </main>
<%- include('partials/footer')%>
<script src="https://code.jquery.com/jquery-3.6.0.min.js" integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4=" crossorigin="anonymous"></script>
<script src="/js/userChangePassword.js"></script>
</body>
</html>